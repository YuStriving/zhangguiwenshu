# 项目架构文档

## 1. 项目概述

### 1.1 项目简介
**zhangguiwenshu** 是一个基于Python的元数据知识图谱构建系统，主要用于数据仓库的元数据管理和知识图谱构建。

### 1.2 核心功能
- 元数据管理（表、列、指标信息）
- 向量化存储和检索
- 全文搜索支持
- 数据仓库元数据同步

### 1.3 技术栈
- **后端框架**: Python + FastAPI (待实现)
- **数据库**: MySQL (元数据存储)
- **向量数据库**: Qdrant
- **搜索引擎**: Elasticsearch
- **嵌入服务**: BGE-Large-ZH 模型
- **容器化**: Docker + Docker Compose

## 2. 系统架构

### 2.1 整体架构图
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   数据仓库层     │    │   元数据层      │    │   知识图谱层     │
│  (MySQL DW)     │◄──►│  (MySQL Meta)   │◄──►│  (Qdrant + ES)  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                   应用服务层 (Python FastAPI)                    │
└─────────────────────────────────────────────────────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   数据同步模块   │    │   元数据服务    │    │   搜索服务      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 2.2 核心模块说明

#### 2.2.1 数据访问层 (Repositories)
- **meta_mysql_repository**: 元数据MySQL存储库
- **dw_mysql_repository**: 数据仓库MySQL存储库

#### 2.2.2 客户端管理层 (Clients)
- **mysql_client_manager**: MySQL客户端管理
- **qdrant_client_manager**: Qdrant向量数据库管理
- **es_client_manager**: Elasticsearch搜索管理
- **embedding_client_manager**: 嵌入服务管理

#### 2.2.3 业务逻辑层 (Services)
- **meta_knowledge_service**: 元数据知识服务

#### 2.2.4 数据模型层 (Models & Entities)
- **Models**: SQLAlchemy ORM模型
- **Entities**: 纯数据实体类

## 3. 数据库设计

### 3.1 元数据库表结构

#### 3.1.1 table_info (表信息表)
```sql
CREATE TABLE table_info (
    id          VARCHAR(64) PRIMARY KEY COMMENT '表编号',
    name        VARCHAR(128) COMMENT '表名称',
    role        VARCHAR(32) COMMENT '表类型(fact/dim)',
    description TEXT COMMENT '表描述'
);
```

#### 3.1.2 column_info (列信息表)
```sql
CREATE TABLE column_info (
    id          VARCHAR(64) PRIMARY KEY COMMENT '列编号',
    name        VARCHAR(128) COMMENT '列名称',
    type        VARCHAR(64) COMMENT '数据类型',
    role        VARCHAR(32) COMMENT '列类型',
    examples    JSON COMMENT '数据示例',
    description TEXT COMMENT '列描述',
    alias       JSON COMMENT '列别名',
    table_id    VARCHAR(64) COMMENT '所属表编号'
);
```

#### 3.1.3 metric_info (指标信息表)
```sql
CREATE TABLE metric_info (
    id               VARCHAR(64) PRIMARY KEY COMMENT '指标编码',
    name             VARCHAR(128) COMMENT '指标名称',
    description      TEXT COMMENT '指标描述',
    relevant_columns JSON COMMENT '关联的列',
    alias            JSON COMMENT '指标别名'
);
```

#### 3.1.4 column_metric (列指标关联表)
```sql
CREATE TABLE column_metric (
    column_id VARCHAR(64) COMMENT '列编号',
    metric_id VARCHAR(64) COMMENT '指标编号',
    PRIMARY KEY (column_id, metric_id)
);
```

## 4. 核心业务流程

### 4.1 元数据构建流程
```python
# 1. 读取配置文件
config = load_config("meta_config.yaml")

# 2. 同步数据仓库表结构
table_columns = dw_repository.get_table_columns_details(table_name)

# 3. 构建元数据对象
meta_table = TableInfo(id=table_name, name=table_name, ...)
meta_column = ColumnInfo(id=column_id, name=column_name, ...)

# 4. 持久化到数据库
meta_repository.insert_meta_table([meta_table])
meta_repository.insert_meta_columns([meta_columns])

# 5. 构建向量索引
embedding_service.create_vector_index(meta_data)

# 6. 构建全文索引
search_service.create_search_index(meta_data)
```

### 4.2 数据查询流程
```python
# 1. 用户输入查询
query = "用户表的年龄列"

# 2. 向量化查询
query_vector = embedding_service.embed_query(query)

# 3. 向量检索
vector_results = qdrant_client.search(query_vector)

# 4. 全文检索
text_results = es_client.search(query)

# 5. 结果融合和排序
final_results = merge_results(vector_results, text_results)

# 6. 返回详细元数据信息
return get_metadata_details(final_results)
```

## 5. 配置管理

### 5.1 应用配置 (app_config_local.yaml)
```yaml
logging:
  file:
    enable: true
    level: INFO
    path: logs
    rotation: "10 MB"
    retention: "7 days"
  console:
    enable: true
    level: INFO

db_meta:
  host: localhost
  port: 3306
  user: atguigu
  password: Atguigu.123
  database: meta

db_dw:
  host: localhost
  port: 3306
  user: atguigu
  password: Atguigu.123
  database: dw

qdrant:
  host: localhost
  port: 6333
  embedding_size: 1024

embedding:
  host: localhost
  port: 8081
  model: BAAI/bge-large-zh-v1.5

es:
  host: localhost
  port: 9200
  index_name: data_agent

llm:
  model_name: qwen-plus
  api_key: your_api_key_here
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
```

### 5.2 元数据配置 (meta_config.yaml)
```yaml
tables:
  - name: "user_table"
    role: "dim"
    description: "用户维度表"
    columns:
      - name: "user_id"
        role: "primary_key"
        description: "用户ID"
        alias: ["用户编号", "用户标识"]
      - name: "age"
        role: "dimension"
        description: "用户年龄"
        alias: ["年龄", "岁数"]

metrics:
  - name: "user_count"
    description: "用户数量统计"
    relevant_columns: ["user_id"]
    alias: ["用户总数", "用户数量"]
```

## 6. 部署架构

### 6.1 容器化部署
使用Docker Compose管理所有服务：
- MySQL (元数据 + 数据仓库)
- Qdrant (向量数据库)
- Elasticsearch (搜索引擎)
- Embedding Service (嵌入服务)
- Application Service (应用服务)

### 6.2 环境要求
- Python 3.8+
- Docker & Docker Compose
- 至少8GB内存
- 50GB存储空间

## 7. 扩展性设计

### 7.1 水平扩展
- 数据库读写分离
- 向量数据库分片
- 搜索服务集群

### 7.2 功能扩展
- 插件化架构设计
- 可配置的元数据源
- 自定义嵌入模型支持

---

**版本**: 1.0  
**作者**: AI生成  
**更新日期**: 2026-04-13